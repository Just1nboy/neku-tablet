/* Filled in by whoever deploys this app (see SETUP.md) — the artist never touches it.
   clientId: the "Web application" OAuth client ID from the Google Cloud project.
   With this filled, the app opens straight to the upload screen — no setup questions. */
window.NEKU_CONFIG = {
  clientId: '',
  stagingFolder: 'Sprite Staging',
};
